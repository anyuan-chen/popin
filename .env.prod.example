# Production secrets for Popin. Copy to .env.prod and fill in real values.
# This file is gitignored. Keep it on the host only (never commit).
#
# Generate a key pair with:
#   docker run --rm livekit/livekit-server generate-keys

LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=